package net.fabric_extras.ranged_weapon.client;

public class RangedWeaponAPIClient {
    /**
     * Runs the mod initializer on the client environment.
     * Item model properties are registered by the platform entrypoints
     * (Fabric: directly here, NeoForge: `RegisterRangeSelectItemModelPropertyEvent`).
     */
    public static void init() {
    }
}
